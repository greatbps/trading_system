# 고도화된 매도 전략 가이드

매매조건고도화.md 기반으로 개발된 고도화된 매도 전략 시스템 사용 가이드

## 🎯 개요

기존의 단순한 **익절 +6% / 손절 -3%** 조건을 다음과 같이 고도화했습니다:

### ✨ 주요 개선사항

1. **부분 익절 (Scale-out)**: 단계별 수익실현으로 리스크 분산
2. **ATR 기반 트레일링 스탑**: 변동성을 고려한 동적 손절선
3. **볼륨/VWAP 필터**: 가짜 신호 방지
4. **시간 필터**: 장 마감 전 리스크 관리
5. **Optuna 자동 최적화**: 매개변수 자동 탐색
6. **변동성 기반 동적 조정**: 시장 상황에 맞춘 파라미터 조정

---

## 📊 새로운 매도 전략 구조

### 1단계: 하드 스탑 (-3%)
```
현재가 <= 진입가 * 0.97 (기본값)
→ 전량 즉시 매도 (시장가)
```

### 2단계: 1차 부분익절 (+4%)
```
현재가 >= 진입가 * 1.04
→ 보유량의 40% 매도 (지정가)
```

### 3단계: 2차 부분익절 (+6%)
```
현재가 >= 진입가 * 1.06
→ 보유량의 40% 추가 매도 (지정가)
→ ATR 트레일링 스탑 활성화 (잔여 20%)
```

### 4단계: ATR 트레일링 스탑
```
트레일링 라인 = 최고가 - (ATR * 1.5배수)
현재가 <= 트레일링 라인
→ 잔여량 전량 매도 (시장가)
```

### 5단계: EMA + 볼륨 브레이크다운
```
+6% 이상 수익 상태 AND
현재가 < 3분봉 5기간 EMA AND
거래량 > 평균거래량 * 1.2배
→ 잔여량 전량 매도 (시장가)
```

### 6단계: 시간 필터
```
장 마감 30분 전 (15:00 이후)
→ 모든 포지션 강제 청산
```

---

## 🔧 시스템 구성 요소

### 1. AdvancedExitStrategy (고도화 매도 전략)
**파일**: `strategies/advanced_exit_strategy.py`

**주요 기능**:
- 포지션별 매도 신호 분석
- ATR 기반 트레일링 스탑 관리
- 부분 익절 로직 처리
- 시간/볼륨 필터 적용

**사용법**:
```python
from strategies.advanced_exit_strategy import AdvancedExitStrategy

strategy = AdvancedExitStrategy()

# 포지션 정보 업데이트
await strategy.update_position(symbol, holding_data)

# 매도 신호 분석
signals = await strategy.analyze_exit_signals(symbol, market_data)
```

### 2. ExitSignalExecutor (매도 신호 실행기)
**파일**: `strategies/exit_signal_executor.py`

**주요 기능**:
- 매도 신호 실제 실행
- 부분 매도 처리
- 주문 결과 추적

**사용법**:
```python
from strategies.exit_signal_executor import ExitSignalExecutor

executor = ExitSignalExecutor(config, kis_collector, trading_executor)

# 매도 신호 실행
result = await executor.execute_exit_signal(symbol, exit_signal)
```

### 3. ExitStrategyOptimizer (자동 최적화)
**파일**: `optimization/exit_strategy_optimizer.py`

**주요 기능**:
- Optuna 기반 매개변수 최적화
- 백테스트 및 성능 평가
- 변동성 기반 동적 조정

**사용법**:
```python
from optimization.exit_strategy_optimizer import ExitStrategyOptimizer

optimizer = ExitStrategyOptimizer()

# 최적화 실행
result = await optimizer.optimize_parameters(historical_data, n_trials=100)
```

---

## 🚀 사용 방법

### 1. 기본 설정

기존 시스템에 자동으로 통합됩니다. 7번 자동매매 > 3번 모니터링에서 고도화된 매도 신호를 확인할 수 있습니다.

### 2. 테스트 실행

```bash
# 고도화된 매도 전략 종합 테스트
python test_advanced_exit_strategy.py
```

### 3. 파라미터 조정

`strategies/advanced_exit_strategy.py`의 `params` 딕셔너리에서 조정:

```python
self.params = {
    'hard_stop_loss': 0.97,      # -3% 하드 스탑
    'soft_target': 1.06,         # +6% 소프트 타깃
    'partial_tp_level1': 1.04,   # +4% 1차 부분익절
    'partial_tp_level2': 1.06,   # +6% 2차 부분익절
    'partial_ratio_1': 0.4,      # 1차에서 40% 매도
    'partial_ratio_2': 0.4,      # 2차에서 40% 매도
    'atr_multiplier': 1.5,       # ATR 배수
    'volume_threshold': 1.2,     # 볼륨 임계값
}
```

### 4. 자동 최적화 실행

```python
# 최적화 스크립트 실행
from optimization.exit_strategy_optimizer import ExitStrategyOptimizer

optimizer = ExitStrategyOptimizer()
result = await optimizer.optimize_parameters(
    historical_data=your_data,
    n_trials=50  # 최적화 시도 횟수
)

print(f"최적 파라미터: {result.best_params}")
```

---

## 📈 성능 개선 효과

### 기존 시스템 vs 고도화 시스템

| 구분 | 기존 시스템 | 고도화 시스템 |
|------|-------------|---------------|
| 익절 방식 | 일괄 매도 (+6%) | 단계별 부분 익절 |
| 손절 방식 | 고정 손절 (-3%) | ATR 트레일링 스탑 |
| 추세 대응 | 정적 | 동적 (ATR 기반) |
| 노이즈 필터 | 없음 | 볼륨/VWAP 필터 |
| 최적화 | 수동 | 자동 (Optuna) |
| 시간 관리 | 없음 | 장 마감 전 청산 |

### 예상 개선 효과

1. **수익 극대화**: 부분 익절로 트렌드 연장 시 추가 수익
2. **리스크 감소**: 트레일링 스탑으로 하락장 대응력 향상
3. **오신호 방지**: 볼륨 필터로 가짜 브레이크다운 방지
4. **최적화**: 자동 매개변수 탐색으로 지속적 성능 개선

---

## ⚠️ 주의사항

### 1. 백테스트 검증 필수
- 실전 적용 전 충분한 백테스트 실행
- 다양한 시장 환경에서 검증
- 슬리피지/수수료 실제 반영

### 2. 과최적화 위험
- 특정 기간에만 유리한 파라미터 주의
- 정기적인 재최적화 필요
- Walk-forward 검증 권장

### 3. 실거래 차이
- 백테스트와 실거래 결과 차이 발생 가능
- 페이퍼 트레이딩으로 사전 검증
- 소량으로 시작하여 점진적 확대

### 4. 시장 환경 변화
- 변동성이 급변하는 시장에서 성능 저하 가능
- 정기적인 파라미터 점검 필요
- 시장 상황에 따른 전략 조정

---

## 🛠️ 문제해결

### 1. 매도 신호 미발생
```python
# 파라미터 확인
print(strategy.params)

# 포지션 상태 확인
print(strategy.get_position_summary())

# 시장 데이터 확인
market_data = await handler._get_market_data_for_exit(symbol)
print(market_data)
```

### 2. 트레일링 스탑 오작동
```python
# ATR 계산 확인
atr_value = await strategy._calculate_atr(symbol, market_data)
print(f"ATR: {atr_value}")

# 트레일링 스탑 업데이트
await strategy.update_trailing_stops()
```

### 3. 최적화 실패
```python
# 히스토리컬 데이터 검증
print(f"데이터 건수: {len(historical_data)}")
print(f"데이터 형식: {historical_data[0] if historical_data else 'None'}")

# 파라미터 범위 조정
optimizer.param_ranges['atr_multiplier'] = (1.0, 2.0, 0.1)  # 범위 축소
```

---

## 📞 지원 및 문의

시스템 관련 문의나 버그 리포트는 다음 방법으로 연락해주세요:

1. **로그 파일 확인**: `logs/` 폴더의 최신 로그
2. **테스트 실행**: `python test_advanced_exit_strategy.py`
3. **상세 로깅 활성화**: 디버그 모드 로그 레벨 설정

---

## 📚 참고 자료

1. **매매조건고도화.md**: 원본 ChatGPT 분석 문서
2. **ATR 지표 이해**: 평균진폭지표 기본 개념
3. **Optuna 문서**: 하이퍼파라미터 최적화 라이브러리
4. **백테스팅 가이드**: 전략 검증 방법론

---

**🎉 고도화된 매도 전략으로 더 안전하고 수익성 높은 거래를 경험하세요!**